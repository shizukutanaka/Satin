# プロダクト総棚卸し — ソクラテス問答法 × イーロン・マスク思考法

本文書は、Satin の**長所・短所・改善点**を 2 つの手法で洗い出した記録である。

- **ソクラテス問答法**: 主張を鵜呑みにせず、「本当にそうか?」「その根拠は?」
  「反例は?」と問いを重ね、検証で答えられたものだけを残す。答えは断定ではなく
  実測・テスト・コードへの参照で裏付ける。
- **イーロン・マスク思考法（5 ステップアルゴリズム）**: 各短所への処方を
  次の 5 段階（順序厳守）のどれかに割り当てる。
  1. 要件を疑う (make the requirements less dumb)
  2. 部品・工程を削除する (delete the part or process)
  3. 簡素化・最適化する (simplify and optimize)
  4. サイクルタイムを短縮する (accelerate cycle time)
  5. 自動化する (automate)

  削除の前に簡素化しない・自動化は最後、という順序自体が方法の核心である。
  適用の詳細な経緯は `AGENT_WORKORDERS.md` の
  「イーロン・マスクの 5 ステップアルゴリズム」の節を参照。

---

## 0. 前提の吟味 — このプロダクトは何であるべきか

**問**: Satin は誰の、何を解決するプロダクトか?

**答**: ローカルで動く 3D アバターコンパニオン。会話・好感度・気分追跡・TTS・
ダッシュボードを、**LLM もクラウドも使わずに**提供する。

**反問**: 2026 年に LLM を使わないコンパニオンとは、単に未完成なのではないか?

**吟味**: 違う、と `AGENT_WORKORDERS.md` §0.1 が明示する。ノー LLM・オフライン・
プライバシー第一は**意図した設計**であり、「LLM を足せば会話が良くなる」類の
提案は Tier C（人間の承認が要る）である。会話データが端末を出ないことは、
API キー不要・従量課金なし・検閲なしと並ぶ、この製品の交換不能な性質である。
よってテンプレートベースの応答は「短所」の欄ではなく設計制約の欄に置く。
これがステップ 1（要件を疑う）の最初の適用であり、同時に限界でもある —
疑った結果「この要件は正しい」と確認される要件も存在する。

**問**: では「完成」とは何を指すか?

**答**: 謳っている機能が実機で動き、動くことが自動検証され、ユーザーを
操作的に扱わず、ドキュメントが実態と一致している状態。以降の節はこの定義に
照らして採点する。

---

## 1. 長所 — 検証で裏付けられたものだけ

**問**: 長所として挙げるものは、希望ではなく事実か?

**答**: 以下はすべて本リポジトリのテストまたは実測で裏付けられている。

| 長所 | 裏付け |
|---|---|
| 検証ゲートが 1 コマンド | `check.py`（9 項目: コンパイル・ruff・mypy・pytest・設定検証・4 スモーク）。CI と手元で同一条件 |
| 型検査に免除ゼロ | `mypy.ini` に `ignore_errors` セクションが 1 つも無く、`main/` 全 38 モジュールが対象 |
| 任意依存が欠けても縮退して起動 | 射影行列から GLU を排除（`main/gl_widget_base.py`）、numpy 無しでも移動可（`main/autonomous_behavior.py`）、各 import が try/except で防御 |
| 操作的パターンへのガードレール | 別れ際の引き留め検出（`main/farewell_integrity.py`）、深夜・過集中利用への休息の促し（`main/usage_guardrails.py`）、初日で愛を告白しない下限（`main/mood.py` の confession floor） |
| 法定 AI 開示 | 起動時 + 3 時間ごとの「AI である」開示（`main/ai_disclosure.py`、NY AI Companion Models 法 / CA SB 243 対応）。`tests/test_ai_disclosure.py` |
| データ主権 | 個人データは**エクスポートを含めて** 0600（`main/fsutil.atomic_write_text(restrict=True)`）、`/forget-all`(CLI)・`/forget-me`(GUI) が確認つきで全消去（`main/data_erasure.py`）、クラウド送信コードは存在しない（W-04 で全削除）。§2 #6 で反証したうえで真にした |
| GUI と CLI の文言が単一の出どころ | `command_usage()` 等を両者が共有し、`tests/test_command_usage_parity.py` がドリフトを赤にする |
| ドキュメントの死参照が自動検出される | `tests/test_docs_references.py` が実在しない .py 参照とリンク切れを検査 |
| i18n の ja/en キー完全一致 | `main/i18n/locales/` を `tests/test_i18n.py` が突き合わせ |

**反問**: このリストに反例は無いか? — 「縮退して起動」は 2026-08 時点まで
偽だった（GLU 欠如で abort）。現在真なのは修正とテストが入ったからであり、
リストの各行は対応するテストが赤くなれば長所の資格を失う。

---

## 2. 短所 — 実測で確認された弱点

**問**: 最も痛い弱点は何か?

**答**: 検証の穴と、既定値の設計判断である。列挙する（すべて実測ベース）。

1. **CI が有効化されていない。** `setup/github-actions-ci.yml` は用意済みだが
   `.github/workflows/` への配置はリポジトリオーナーにしかできない
   （エージェントのトークンには workflows 権限が無い）。CI 不在の期間、
   テストスイートは「PyQt5 が無い環境でだけ」通っており、実環境では
   197 件落ちていた — これは CI があれば初日に赤くなっていた。
2. **GUI の実描画・実操作の検証が手動だった。** 自律モードのアバターが
   画面外へ歩き去る欠陥（コミット b2b28ac）は、xvfb で実際に起動して
   座標を実測するまで、どの静的検査にも掛からなかった。同型の欠陥は
   今後も「手で起動した者」しか見つけられない。
3. ~~**関係ペーシングの既定値が速い。**~~ **解決済み（オーナー判断）。**
   好感度は初期 50・最高レベル閾値 80 で、日次上限が 30 だったため初日
   8 メッセージで最高レベルに到達し得た。オーナーの判断で日次上限を 5.0
   （最短 6 日の弧）に変更した。**吟味の副産物**: 上限を下げるだけでは
   目的を達しないことが分かった — プレゼントは `adjust()` を直接呼んで
   上限を迂回しており、7 種の合計 30.5 がちょうど 50→80 を埋めるので、
   上限をいくつにしても初日に最高レベルへ到達できた。稼げる上昇
   （会話・プレゼント）が同じ日次予算を共有するよう `earn()` を導入した。
   **反問（2 周目）**: 「`earn()` を入れた今、本当に初日に届かないか?」 —
   届いた。同型の迂回が 3 つ残っていた（メモリ内で実測）:
   「おやすみ」を 20 回打つと初日に affinity 90 / close（ルーティンボーナスが
   `adjust()`）、ログインボーナス 2.0〜5.0/日が上限の外に積まれて弧が
   4 日で終わる、CLI だけが会話にデイリームード乗数を上限の外で掛けていた
   （GUI には無い = 動作ドリフト）。前 2 者は `earn()` へ、後者は削除。
   `register()` だけを回していた弧テストはどれも捕まえられなかったので、
   実経路（ログイン → 会話、CLI の入力ループ）を回すテストに置き換えた。
4. ~~**`--avatar-loader` が `/avatar` コマンドと機能重複。**~~
   **解決済み（オーナー判断で削除）。** tkinter と Pillow が依存から完全に
   消えた。**吟味の副産物**: 削除したフラグを渡しても argparse がエラーに
   ならないことが分かった — `manage_subargs` の `argparse.REMAINDER` が
   認識できない引数をすべて飲み込んでおり、`--chat` を `--chta` と打っても
   黙って 3D GUI が起動していた。不明な引数を報告するようにした。
6. ~~**「個人データは 0600」が偽だった。**~~ **解決済み。** §1 の長所を自ら
   反証しにいって見つけた。ライブの会話ログ・好感度は確かに 0600 だが、
   **エクスポート 5 経路が 0644 で書いていた** — `/export-log`（CLI・GUI）と
   `manage_satin` の `mood export`・`log export`・`log csv`。いずれも会話全文や
   好感度の**完全な複製**であり、元より弱い権限で置かれていた（実測で確認）。
   製品としての方針は既に決まっていた点が重要で、ダッシュボードの zip
   エクスポートと restore は `restrict_to_owner` を掛けている。5 経路だけが
   取り残されていた。処方は**ステップ 3（簡素化）**: 5 つの独自 `open()` を
   既存の `fsutil.atomic_write_text(..., restrict=True)` 1 つに統一した。
   書き方が 1 つになったことで、CSV の改行が Windows で二重変換される問題
   （`newline=""` を付けていたのは 3 経路中 1 つだけだった）も同時に消えた。
   **反証の失敗も記録しておく**: 同時に「ローテート後のライブログが 0644 に
   戻る」とも疑ったが、これは**誤り**だった。`open(path, "w")` は既存ファイルの
   パーミッションを変えないので 0600 のまま。実測しなければ、直す必要のない
   ものを直していた。
7. **一人の人間による審査を経ていない。** ガードレールの閾値（深夜の定義、
   休息を促す頻度）や台詞の質は、テストで固定できても妥当性は検証できない。

**反問**: 「テンプレート応答は多様性に欠ける」を短所に入れないのはなぜか?
— §0 で吟味したとおり、それは設計制約である。制約の中での質（台詞の
バリエーション、文脈適合）は改善対象であり、実際に本セッションで
共感応答（`main/everyday_distress.py`）等を追加した。

---

## 3. 改善点 — 5 ステップへの割り当て

**問**: 各短所に、どのステップをどの順で適用するか?

| # | 短所 | ステップ | 処方 | Tier |
|---|---|---|---|---|
| 1 | CI 未有効化 | 5. 自動化 | オーナーが `setup/github-actions-ci.yml` を `.github/workflows/ci.yml` へコピーする（1 分作業）。それまでの代替として `check.py` を毎回実行 | **オーナー作業** |
| 2 | GUI 実描画の検証が手動 | 5. 自動化 | xvfb が使える環境では `check.py` に GUI スモーク（実 Qt で起動→自律モード→座標が可視範囲内→終了）を追加する | **A（本文書と同時に実装）** |
| 3 | ペーシング既定値 | 1. 要件を疑う → 3. 簡素化 → 2. 削除 | **決定済み**: 日次上限を 5.0（最短 6 日の弧）へ。稼げる上昇（会話・プレゼント・ルーティン・ログイン）を例外なく 1 つの日次予算に統合（`earn()`）— 予算が分かれていると上限が弧の長さを決めない。CLI 限定のムード乗数は削除 | ✅ 実施済み |
| 4 | `--avatar-loader` 重複 | 2. 削除 | **決定済み**: 削除。`/avatar` が同じ選択を本体内で提供する。tkinter・Pillow 依存も消滅。あわせて未知の引数を黙って飲み込む挙動を修正 | ✅ 実施済み |
| 6 | エクスポートが 0644 | 3. 簡素化 | **完了**: 5 経路の独自 `open()` を `fsutil.atomic_write_text(restrict=True)` へ統一。権限と改行が同時に正しくなる | ✅ 実施済み |
| 7 | 人間の審査なし | 1. 要件を疑う | 台詞・閾値はコード審査ではなく製品審査の対象。本文書と `SPECIFICATION.md` の安全設計の章が審査の入口 | オーナー作業 |

適用順序が方法どおりであることに注意 — #3・#5 は「そもそも要件が正しいか」
（ステップ 1）で止めてあり、正しさが確認される前に最適化や自動化はしない。
#2 だけが今すぐ実装可能な自動化（ステップ 5）であり、対象の挙動
（境界反射）が先にテストで固定済みだから最後のステップに進める。

---

## 4. 結論

**問**: §0 の「完成」の定義に照らして、いまの Satin はどうか?

**答**: 機能は実機で動き（自律モード・TTS・ダッシュボード・消去を xvfb /
実行で確認）、動くことは `check.py` の 9 項目 + テストスイートが自動検証し、
操作的パターンには検出と抑止が入り、ドキュメントの死参照はテストが防ぐ。
残る非完成要素は上表のうち #1（CI 有効化）と #7（人間による製品審査）だけで、
どちらも人間の権限・判断を要する。本文書がその判断材料である。

**方法についての一言**: §2 の #3・#4・#6 はいずれも「解決済み」と書いた項目を
**もう一度疑って**見つかったものである。#6 では反証が 1 つ外れた（ローテート後の
権限）ことも記録した — ソクラテス問答法の要点は正しく当てることではなく、
実測で答えられる形に問いを変えることにある。
