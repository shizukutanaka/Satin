# mypy 設定 — 静的型検査の段階導入（ワークオーダー W-07）
#
# 方針: **新しく書くコードは既定で型検査対象**にし、まだ通らない既存モジュールだけを
# 下に列挙して免除する（グランドファザリング）。列挙する側を「これから直すもの」に
# しておくと、リストは時間とともに**縮む**方向にしか動かない。逆に「検査するもの」を
# 列挙する設計だと、新規モジュールは黙って検査対象外になり、リストを更新し忘れた
# ぶんだけ穴が広がる — `tests/test_i18n.py` の _DASHBOARD_KEYS が実際にそうやって
# 陳腐化した（32 件のハードコードに対し実使用 37 件）。同じ轍は踏まない。
#
# 現状: main/ の 94 モジュール中 **70 が検査対象（クリーン）**、24 を免除
# （導入時は 52/42）。免除リストから 1 行消すたびに検査範囲が広がる。
#
# 実行: リポジトリルートで `python -m mypy`（引数不要。files= が対象を決める）
#
# 背景: ruff は F821（NameError 級）を捕まえてくれるが、型の不整合は実行時まで
# 表に出ない。実際この導入で「フォールバック実装の引数名が本物とずれていて、
# キーワード呼び出しがフォールバック時だけ TypeError になる」既存バグが 2 件
# 見つかっている（user_wellbeing / usage_guardrails の _find_archives）。

[mypy]
files = main
mypy_path = main
# 任意依存（PyQt5・OpenGL・pygltflib・flask・cv2 …）は未導入環境でも検査を
# 通したいので、スタブ不在をエラーにしない。
ignore_missing_imports = True
# 段階導入の要: 注釈の無い関数の**中身**までは検査しない。注釈済みの箇所だけを
# 厳格に見て、未注釈コードは「まだ書かれていない契約」として素通りさせる。
check_untyped_defs = False
disallow_untyped_defs = False
# 到達不能コード・冗長なキャストは設定ミスの兆候なので拾う。
warn_unreachable = False
warn_redundant_casts = True
warn_no_return = True
# 出力を読みやすく（どのコードを無視すればよいかが分かる）。
show_error_codes = True
pretty = False

# --------------------------------------------------------------------------- #
# グランドファザリング: まだ型検査を通らないモジュール。
# **このリストは縮めるためにある。** 直したら該当ブロックを消すこと。
#
# 進め方: `python -m mypy --config-file=/dev/null --ignore-missing-imports
# main/<module>.py` で素の指摘を見てから直す（この設定ファイルを噛ませると
# 免除が効いてしまい何も出ない）。エラー件数の少ないものから着手すると良い。
# --------------------------------------------------------------------------- #

# --- GUI / 3D ウィジェット（`QOpenGLWidget if X is not None else object` の
#     条件付き基底クラスを mypy が解決できないため、まとめて免除）---
[mypy-avatar_3d_autonomous_tts]
ignore_errors = True
[mypy-avatar_3d_autonomous]
ignore_errors = True
[mypy-avatar_3d_autonomous_or_camera]
ignore_errors = True
[mypy-avatar_3d_gltf_viewer]
ignore_errors = True
[mypy-avatar_3d_mic_tts_modes]
ignore_errors = True
[mypy-avatar_3d_sync]
ignore_errors = True
[mypy-avatar_3d_viewer]
ignore_errors = True
[mypy-autonomous_gltf_avatar]
ignore_errors = True
[mypy-autonomous_behavior]
ignore_errors = True

# --- Web / 統合 ---
[mypy-dashboard]
ignore_errors = True
[mypy-web_integrator]
ignore_errors = True
[mypy-content_aggregator]
ignore_errors = True

# --- インフラ / ユーティリティ ---
[mypy-plugin_manager]
ignore_errors = True
[mypy-logging_manager]
ignore_errors = True
[mypy-observability]
ignore_errors = True
[mypy-optimize]
ignore_errors = True
[mypy-code_quality_metrics]
ignore_errors = True
[mypy-performance_profiling]
ignore_errors = True
[mypy-async_optimization]
ignore_errors = True
[mypy-error_handling]
ignore_errors = True
[mypy-config_manager_enhanced]
ignore_errors = True
[mypy-utils_config]
ignore_errors = True
[mypy-config.*]
ignore_errors = True
[mypy-i18n.*]
ignore_errors = True

# --- 対話 / 集計 ---
[mypy-persona_cli]
ignore_errors = True
[mypy-daily_mood]
ignore_errors = True
